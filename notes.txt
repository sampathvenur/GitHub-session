(ignore the "<>" in the commands)
git init => (initialize git repo)
git add . => (add all files to stage)
git add <folder/file name> => (add a particular folder/file to the stage)
git log => (all logs)
git log --oneline => (logs in one line)
git checkout -b 'feature-branch' => (created a branch named 'feature-branch')
git checkout -D 'feature-branch' => (deleted a branch named 'feature-branch')
git checkout <branch-name> => (switch to branches)
git merge <branch-name> => (merging a branch to the main branch)